version: '3.4'
services:
  k6:
    image: grafana/k6:latest
    command: run /api/MedicationDispense/k6_MedicationDispense.js
    volumes:
      - "../../api/MedicationDispense:/api/MedicationDispense"
      - "../../inc:/inc"
    environment: 
      - "ERX_ENV=${ERX_ENV}"
      - "ERX_VUS=${ERX_VUS}"
      - "ERX_ITERATIONS=${ERX_ITERATIONS}"
      - "ERX_CLIENT_SECRET=${ERX_CLIENT_SECRET}"