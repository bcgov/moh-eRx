version: '3.4'
services:
  k6:
    image: grafana/k6:latest
    command: run /api/MedicationRequest/k6_MedicationRequest.js
    volumes:
      - "../../api/MedicationRequest:/api/MedicationRequest"
      - "../../inc:/inc"
    environment: 
      - "ERX_ENV=${ERX_ENV}"
      - "ERX_VUS=${ERX_VUS}"
      - "ERX_ITERATIONS=${ERX_ITERATIONS}"
      - "ERX_CLIENT_SECRET=${ERX_CLIENT_SECRET}"
      - "ERX_CLIENT=${ERX_CLIENT}"